/* eslint linebreak-style: ["error", "unix"] */
/* eslint no-param-reassign: "error" */
export interface IBook {
  id: number;
  title: string;
  author: string;
  description: string;
  genre: string;
  year: number;
}
